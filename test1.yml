pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        git branch: 'main', url: 'git@github.com:kullanici/repo.git'
      }
    }
    stage('Run Ansible') {
      steps {
        sh 'ansible-playbook playbook.yml'
      }
    }
  }
}
